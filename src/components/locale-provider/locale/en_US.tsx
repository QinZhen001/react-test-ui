import { Locale } from '../types';

const localeValues: Locale = {
  locale: 'en',
  global: {
    placeholder: 'placeholder',
  },
  Text: {
    edit: 'edit',
    copy: 'copy',
    copied: 'copied',
    expand: 'expand',
  },
};

export default localeValues;
